# Codeception Test Suite Configuration

actor: FunctionalTester
modules:
  enabled:
    - Symfony
    - Cli
    - Db:
        dsn: 'sqlite:///:memory'
        user: test
        password: test
    - Doctrine2:
        depends: Symfony
    - \Helper\Functional
    - Asserts
error_level: "E_STRICT"
